# Platform settings route (MES-020)

Canonical UI: `/dashboard/settings` (and subpaths).

This file’s route (`/settings`) redirects to `/dashboard/settings`. Feature: `features/platform-settings`. Shared: `services/settings`.
