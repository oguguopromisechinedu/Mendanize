# Admin guides (MES module)

Per [APP-ROUTER-PATHS.md](../../../docs/APP-ROUTER-PATHS.md), admin content routes that share names with public paths live at:

`app/(dashboard)/dashboard/guides/` → `/dashboard/guides`

Do not add `page.tsx` here — it would collide with `app/(public)/guides`.
