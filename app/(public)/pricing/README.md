# Public pricing (MES-021)

Canonical `/pricing` under the public surface. Feature: `features/billing`. Shared: `services/billing`.

Read-only catalog — Checkout lives on `/dashboard/settings/billing`.
