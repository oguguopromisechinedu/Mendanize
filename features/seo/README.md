# SEO (MES-015)

Centralized SEO Center — global settings, templates, redirects, robots, sitemap config, structured data previews, and reusable `SeoFieldsPanel`.

## Shared Service

`services/seo` — resolveMetadata + CMS persistence.
