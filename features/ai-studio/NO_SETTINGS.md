# No local AI settings

AI configuration is owned exclusively by:
- `features/platform-settings` (UI)
- `services/settings` (storage / accessors)

Do not add a `settings/` or `config/` folder under this feature.
