/**
 * Actions placeholder — features/recommendations
 * Writes to interests/saved land with Personalization (MES-022).
 */

"use server";

export async function placeholderAction() {
  throw new Error(
    "features/recommendations: interest/save mutations belong to MES-022",
  );
}
