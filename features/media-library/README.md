# Media Library (MES-014)

Digital asset management — library, upload, details, collections, categories, Media Picker.

## Shared Service

`services/media` — persistence + placeholder storage provider (Supabase-ready keys).

## Routes

`/dashboard/media`, `/upload`, `/[id]`, `/recent`, `/unused`, `/collections`, `/categories`
