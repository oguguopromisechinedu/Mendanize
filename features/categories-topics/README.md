# categories-topics

**Implements:** MES-009

**Shared Services:** `content` (taxonomy), `seo`, `media` (image placeholders)

Shared Category → Topic taxonomy used by Articles, Guides, and AI Tools.

## Routes

- `/dashboard/categories`, `/new`, `/[id]`, `/[id]/edit`
- `/dashboard/topics`, `/new`, `/[id]`, `/[id]/edit`
